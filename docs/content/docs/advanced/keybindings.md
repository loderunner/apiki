---
title: "Keybindings"
weight: 1
---

## Main List

### Navigation

| Key | Action |
|-----|--------|
| `↑` / `k` | Move cursor up |
| `↓` / `j` | Move cursor down |
| `/` | Open filter/search |

### Actions

| Key | Action |
|-----|--------|
| `Space` | Toggle selection |
| `+` | Create new variable |
| `=` | Edit variable (or save .env variable permanently) |
| `-` / `Delete` / `Backspace` | Delete variable |
| `i` | Import from environment |
| `Enter` | Apply changes and quit |
| `q` / `Ctrl+C` | Quit without applying |
| `Esc` | Clear filter |

## Filter Mode

After pressing `/`:

| Key | Action |
|-----|--------|
| Type | Search (fuzzy matching) |
| `Enter` | Apply filter and return to list |
| `Esc` | Clear filter and exit |

## Form (Create/Edit)

| Key | Action |
|-----|--------|
| `Tab` / `↓` | Next field |
| `Shift+Tab` / `↑` | Previous field |
| `Enter` | Save (on last field) |
| `Esc` | Cancel |

## Dialogs

When confirming deletion or other actions:

| Key | Action |
|-----|--------|
| `y` / `Enter` | Confirm |
| `n` / `Esc` | Cancel |

## Import Mode

After pressing `i`:

| Key | Action |
|-----|--------|
| `↑` / `↓` / `j` / `k` | Navigate |
| `Space` | Toggle selection |
| `Enter` | Confirm import |
| `Esc` | Cancel and return to main list |
